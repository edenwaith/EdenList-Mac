{
    IBClasses = (
        {
            ACTIONS = {deleteAllRecords = id; deleteRecord = id; deselectAllRecords = id; }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {checkForNewVersion = id; goToProductPage = id; sendFeedback = id; }; 
            CLASS = GeneralController; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}